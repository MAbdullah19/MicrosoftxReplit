node_modules/
dist/
client/dist/
drizzle/
.env
.env*
!.env.example
*.log
.DS_Store

# Replit workspace config holds shared env values — never commit
.replit
# Replit agent scratch (skills, tasks, state) — tooling, not project source
.local/
# Screenshots dropped in the repo root while debugging setup
Screenshot*.png
